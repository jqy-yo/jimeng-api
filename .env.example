# Jimeng API 环境变量配置示例
# 使用方法：将此文件复制为 .env 并修改配置

# ========================================
# Session ID 配置（必需）
# ========================================

# 支持多个 Session ID，用逗号分隔，系统会自动轮询
# 格式：Bearer token1,token2,token3
#
# 国内站 Session ID（从 jimeng.jianying.com 获取）
# 直接填写 token 值，例如：
# JIMENG_SESSION_IDS=your_session_id_1,your_session_id_2,your_session_id_3

# 国际站 Session ID（从 dreamina.capcut.com 获取）
# 需要添加 us- 前缀，例如：
# JIMENG_SESSION_IDS=us-your_session_id_1,us-your_session_id_2

# 混合使用（国内+国际）
# JIMENG_SESSION_IDS=cn_token1,us-us_token1,cn_token2,us-us_token2

# 您的配置（取消注释并填写）：
# JIMENG_SESSION_IDS=

# ========================================
# 服务配置（可选）
# ========================================

# 服务端口（默认：5100）
# PORT=5100

# 服务主机（默认：0.0.0.0，监听所有网卡）
# HOST=0.0.0.0

# 服务名称（默认：jimeng-api）
# SERVICE_NAME=jimeng-api

# ========================================
# 日志配置（可选）
# ========================================

# 启用请求日志（默认：true）
# REQUEST_LOG=true

# 日志目录（默认：./logs）
# LOG_DIR=./logs

# 日志写入间隔（毫秒，默认：200）
# LOG_WRITE_INTERVAL=200

# 日志文件过期时间（毫秒，默认：2626560000，约1个月）
# LOG_FILE_EXPIRES=2626560000

# ========================================
# 临时文件配置（可选）
# ========================================

# 临时文件目录（默认：./tmp）
# TMP_DIR=./tmp

# 临时文件过期时间（毫秒，默认：86400000，1天）
# TMP_FILE_EXPIRES=86400000

# ========================================
# 轮询配置（可选，高级设置）
# ========================================

# 最大轮询次数（默认：900，约15分钟）
# MAX_POLL_COUNT=900

# 轮询间隔（毫秒，默认：5000，5秒）
# POLL_INTERVAL=5000

# 超时时间（秒，默认：900，15分钟）
# TIMEOUT_SECONDS=900

# ========================================
# 重试配置（可选，高级设置）
# ========================================

# 最大重试次数（默认：3）
# MAX_RETRY_COUNT=3

# 重试延迟（毫秒，默认：5000，5秒）
# RETRY_DELAY=5000

# ========================================
# 使用示例
# ========================================

# 示例 1: 单个国内站 Session ID
# JIMENG_SESSION_IDS=abcdef123456

# 示例 2: 多个国内站 Session ID（轮询）
# JIMENG_SESSION_IDS=token1,token2,token3

# 示例 3: 单个国际站 Session ID
# JIMENG_SESSION_IDS=us-abcdef123456

# 示例 4: 多个国际站 Session ID（轮询）
# JIMENG_SESSION_IDS=us-token1,us-token2,us-token3

# 示例 5: 混合使用国内站和国际站（推荐）
# JIMENG_SESSION_IDS=cn_token1,cn_token2,us-us_token1,us-us_token2

# ========================================
# Session ID 获取方法
# ========================================

# 1. 访问即梦官网（国内站）：https://jimeng.jianying.com
#    或国际站：https://dreamina.capcut.com
#
# 2. 登录账号
#
# 3. 打开浏览器开发者工具（F12）
#
# 4. 切换到 Network（网络）标签
#
# 5. 刷新页面或发起任意请求
#
# 6. 找到任意请求，查看 Request Headers
#
# 7. 在 Cookie 字段中找到 sessionid 的值
#
# 8. 复制 sessionid 的值
#    - 国内站：直接使用
#    - 国际站：添加 us- 前缀

# ========================================
# 注意事项
# ========================================

# 1. Session ID 会过期，需要定期更新
# 2. 不要泄露您的 Session ID
# 3. 建议使用多个账号轮询，避免单账号频繁请求
# 4. 国际站 token 必须添加 us- 前缀
# 5. 多个 token 之间用英文逗号分隔，不要有空格
